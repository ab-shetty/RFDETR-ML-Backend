PYTORCH_ENABLE_MPS_FALLBACK=1
LABEL_STUDIO_API_KEY=your_api_key_here
PROJECT_ID=7
LABEL_STUDIO_HOST=http://localhost:8080       # used by train_local.py (local Python runs)
# LABEL_STUDIO_DOCKER_HOST=http://host.docker.internal:8080  # used by Docker for inference (defaults to host.docker.internal)
TRAINING_EPOCHS=10
MODEL_FILE=checkpoint_best_total.pth
